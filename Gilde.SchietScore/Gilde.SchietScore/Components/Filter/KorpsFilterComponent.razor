@* @using Gilde.SchietScore.Models
<div class="dropdown">
    <button class="btn btn-primary dropdown-toggle" type="button" id="korpsDropdown" data-bs-toggle="dropdown" aria-expanded="false">
        @SelectedKorps.Name
    </button>
    <ul class="dropdown-menu" aria-labelledby="korpsDropdown">
        @foreach (var korps in KorpsList.AllKorps)
        {
            <li><a class="dropdown-item" @onclick="@(() => OnKorpsSelection(korps))">@korps.Name</a></li>
        }
    </ul>
</div> *@